import { style } from '@vanilla-extract/css';

export const appWrapper = style({
  width: '100vw',
  height: '100dvh',
  maxWidth: 700,
  backgroundColor: '#fff',
  display: 'flex',
  flexDirection: 'column',
});
